 // number
 function addtwo(num:number){
   return num+2;
}
addtwo(3)
// string
function touppercase(val:string){
  return val.toUpperCase()
}
touppercase("samanvith")
//boolean
function signupuser(name:number,email:string,password:boolean){}
signupuser(56374 ,"sam@435",true)
const heros= ["ironman","thor","superheros"]
heros.map((hero):string=>{
   return `hero is ${hero}`
})
// Note: we dont use any type  while declaring the function defintely we should have to declare any variable while using functions in the project
 export default {}